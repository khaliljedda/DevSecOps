apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-laravel
spec:
  rules:
    - host: devops-internership.com
      http:
        paths:
          - path: /
            backend:
              serviceName: empapp-back-service               
              servicePort: 30000

